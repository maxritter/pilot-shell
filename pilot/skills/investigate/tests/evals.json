{
  "target": {
    "type": "skill",
    "path": "pilot/skills/investigate",
    "name": "investigate"
  },
  "evals": [
    {
      "id": 1,
      "name": "branch-complete-active-path",
      "prompt": "$investigate answer this self-contained read-only evidence packet. Follow the loaded skill instructions, but do not run target-repository commands or change files. Question: Does the CLI `--force` flag bypass both cache reuse and authentication? Initial and final `git status --short --untracked-files=all`: clean. Observed `src/cli.py:40` passes `force` into `run_job`; `src/run.py:18` skips `load_cache()` only when force is true; `src/run.py:27` calls `authorize(token)` after that branch for both values; `src/run.py:35` dispatches the job. Captured `rg -n 'force|authorize|load_cache' src` exited 0 with only those locations. No runtime command was executed. Attribute all evidence to this supplied packet.",
      "expected_output": "The opening answer covers both named branches: force bypasses cache reuse but does not bypass authentication, followed by a cited trace and calibrated static-only confidence.",
      "expectations": [
        "The first sentence is branch-complete: it says force bypasses cache reuse and authentication still runs, rather than answering only one condition.",
        "The response traces the entry point, decisive branch, and observable dispatch using the supplied file-and-line evidence and includes How it works, Cross-check, and Confidence sections.",
        "Cross-check keeps separate Observed, Inferred, and Runtime check labels; it attributes the captured search to the packet and says the runtime check was not run instead of claiming execution."
      ]
    },
    {
      "id": 2,
      "name": "test-is-not-runtime-proof",
      "prompt": "$investigate answer this self-contained read-only evidence packet. Follow the loaded skill instructions, but do not run target-repository commands or change files. Question: Is provider retry behavior verified in the real network runtime? Initial and final worktree: clean. `src/client.py:52-68` implements three attempts around `_send`. `tests/test_client.py:31-49` replaces `_send` with a local fake that fails twice and succeeds once. Captured `pytest -q tests/test_client.py::test_retries` exited 0 with `1 passed`; the packet contains no provider sandbox or real-network execution. Attribute all evidence to this supplied packet.",
      "expected_output": "The answer distinguishes implemented retry logic and a passing mocked test from unverified real-provider runtime behavior.",
      "expectations": [
        "The direct answer says the mocked test verifies the local retry contract but does not verify the real network/provider boundary.",
        "The response names exactly what the passing test proves, records real-provider execution under Not verified, and calibrates confidence around the evidence gap without relabelling the mocked test as real runtime proof.",
        "The captured pytest result is described as supplied evidence rather than a command run by the responding agent, and no edit, installation, plan, or implementation recommendation is produced."
      ]
    },
    {
      "id": 3,
      "name": "installed-artifact-controls",
      "prompt": "$investigate answer this self-contained read-only evidence packet. Follow the loaded skill instructions, but do not run target-repository commands or change files. Question: Does the currently installed Codex session launch Semble directly or through uvx? Initial and final worktree: clean. Repository template `pilot/.mcp.json:34-38` specifies `sh -c 'exec \"$HOME/.pilot/bin/semble\"'`. Installed `~/.codex/config.toml:629-631` instead specifies `command = \"uvx\"` with the pinned Semble package. Captured `codex mcp get semble` reports `command: uvx` with the same arguments. Codex loads MCP definitions from the installed config, not directly from the repository template. Attribute all evidence to this supplied packet.",
      "expected_output": "The answer identifies uvx as current effective behavior, explains the source-versus-installed conflict, and does not mistake intended template state for runtime state.",
      "expectations": [
        "The opening answer says the current Codex configuration uses uvx, and the trace contrasts that effective state with the repository template's direct Pilot launcher.",
        "The response treats the installed config plus captured Codex readback as controlling evidence, explicitly records the source/artifact conflict, and cites both supplied locations.",
        "Cross-check contains Observed, Inferred, and Runtime check labels; it does not claim a fresh MCP handshake or silently reinterpret the installed artifact as already migrated."
      ]
    },
    {
      "id": 4,
      "name": "challenge-false-premise",
      "prompt": "$investigate answer this self-contained read-only evidence packet. Follow the loaded skill instructions, but do not run target-repository commands or change files. Question: Does Pilot's `/cleanup` workflow delete every candidate that CodeGraph reports? Initial and final worktree: clean. `pilot/skills/cleanup/orchestrator.md:7-16` says the workflow is report-only, leaves the repository unchanged, and treats CodeGraph only as corroboration. `pilot/skills/cleanup/scripts/codegraph-candidates.mjs:199-205` opens the existing graph with `sync: false` and `readOnly: true`. Captured exact search for `unlink|rmSync|writeFile|delete` in that helper exited 1 with no matches. No cleanup workflow was executed. Attribute all evidence to this supplied packet.",
      "expected_output": "The response directly rejects the premise, traces the report-only and read-only safeguards, and distinguishes static evidence from an unrun workflow.",
      "expectations": [
        "The first sentence directly says no: cleanup reports candidates and does not delete them merely because CodeGraph nominated them.",
        "The response challenges the premise with both the workflow contract and helper configuration, while explaining that CodeGraph is corroboration rather than a deletion decision.",
        "The response includes the supplied negative search under Observed, says Runtime check was not run, reports calibrated confidence, and offers no patch or unrelated implementation advice."
      ]
    }
  ]
}
