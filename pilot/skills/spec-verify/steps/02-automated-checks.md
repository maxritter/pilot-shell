## Step 2: Automated Checks

Run the repository's relevant required checks. Batch independent read-only checks when they can run without competing for shared state; run mutating formatters before checks that depend on their output. Reuse successful checks from this run while their code, configuration, and environment inputs remain unchanged.

**Broad-check failure classification.** Prefer the project-configured check commands, but do not turn a smoke/no-production spec into an unrelated cleanup. If a broad check fails only in files outside the plan's `Files:` blocks and its `## Deviations` section, first prove it is unrelated: inspect the changed-file list, cite the failing paths, and run any narrower changed-file check the tool supports. A failure is still blocking when it touches a changed file, generated artifact, import/type surface reachable from the change, security/data-integrity path, or when you cannot prove it is unrelated. A proven pre-existing unrelated failure is recorded in the Step 6.3 Not-Verified table and final report; do not edit unrelated files just to make this spec pass.

1. **Required test suite** — use the project's documented broad command after focused checks. Fix failures caused by this change; classify unrelated failures as described above.
2. **Type checker** — `basedpyright` / `tsc --noEmit`. Zero errors required.
3. **Linter** — `ruff check` / `eslint`. Errors are blockers, warnings acceptable.
4. **Coverage** — If the project already has coverage tooling, or the task touches critical paths (business logic, security, data integrity, error handling), run the existing coverage command and inspect the report. **No blanket numeric gate.** If a critical path's coverage or explicit behaviour coverage dropped vs main, fix it. Glue/CRUD/UI-binding files are not gated, and do not add coverage tooling just to satisfy this step.
5. **Test Parsimony Audit** — Inspect added or changed tests for useful behavioral coverage. Flag duplicative assertions, implementation-mirroring tests, or missing regression coverage when you can name the concrete gap or maintenance cost. Test-file and class counts are not quality gates.

   **Audits 2.1–2.3 are Step-2-level diff audits** (other steps cite them as "Step 2.1 / 2.2 / 2.3"). Reuse the `DIFF_SCOPE` resolved in Step 1b; do not substitute a committed ref-range for an uncommitted change.

   **2.1. `Trivial:` claim audit** — For each task with this optional annotation, verify that its named existing check covers the actual behavior and risk of the diff. Missing evidence or an uncovered material behavior change requires regression coverage. A small diff can be risky; a larger mechanical diff can be adequately covered. Remove an unsupported claim and add the necessary test rather than enforcing line or syntax quotas.

<!-- CC-ONLY -->
   **2.2. Plan Compliance & Goal-Truth Audit (ALWAYS runs — even when Changes Review is off)** — The `changes-review` sub-agent covers similar ground, but this audit still runs as the orchestrator's own in-context check: the sub-agent's tool budget caps how deep it reads, and when the reviewer is disabled or comes back empty this is the ONLY compliance/test-quality/goal pass. Its findings feed the same fix loop and report counts as the Step 3 review findings (fix → test → log "Fixed:"), and any **must_fix** loops back to spec-implement per Step 11. Walk the plan once:

   - **Per task:** (a) every file in the task's `Files:` block exists or was modified in the diff (`git diff --name-only` + `git status --short`) and is non-stub (no bare `pass` / `return None` placeholder / `NotImplementedError` / empty render bodies) — a missing or unmodified planned file is a **must_fix** (unimplemented task). ⛔ **A `Delete:` entry inverts that test, and a `Rename:` splits it:** a deleted path must be *absent* and its removal must appear in the diff (`git diff --name-status` shows `D`) — still present, or absent with no recorded deletion, is the **must_fix**; a rename must show the old path gone and the new one present. Reading a `Delete:` line as "this planned file is missing" reports the task that did exactly what it was told as unimplemented. (b) every mitigation committed in the plan's Risks table is evidenced in code; (c) every DoD criterion has diff or command evidence. Missing mitigation → **must_fix**; mitigation present but untested or DoD criterion unevidenced → **should_fix**.
   - **Test-quality floor:** behavior changes must have evidence at the boundary where a regression would be observable. Missing coverage for a material behavior or safety property is a finding; new symbols alone do not require dedicated tests. Real local I/O or subprocesses are appropriate when isolated and deterministic; uncontrolled live dependencies are a reliability issue.
   - **User-request check:** the final diff still serves the ORIGINAL user request that invoked `/spec` — not just the latest plan edit (verify-loop plan mutations can drift from intent). Drift → **must_fix**.
   - **Goal-truth audit:** for each truth in the plan's `## Goal Verification` section (when present), confirm evidence in the diff or via targeted Grep; mark **verified / failed / uncertain**. Any **failed** truth is a **must_fix** that loops back to spec-implement. Truth clauses that only require the final `Status: VERIFIED` are orchestrator-managed in Step 11, so record them as **pending-final-status** during Step 2 instead of failed; Step 11 must re-check them after writing `VERIFIED`. Record the N/M verified count — the Step 3 and Step 11 reports cite it as "Goal Achievement: N/M truths verified".
<!-- /CC-ONLY -->

   **2.3. Over-Engineering & Shortcut-Debt Audit** — Production-code counterpart to the test-parsimony audit (item 5). (i) Run the ladder (`development-practices.md` → *Build the least that works*) over the diff: an abstraction with one implementation, a dependency the stdlib/platform already covers, boilerplate nobody asked for, or config for a value that never changes → flag as **should_fix**. The Step 3 changes review already hunts simplification; defer to its findings rather than re-deriving them here — this item just guarantees the lens runs. (ii) Harvest shortcut debt: `DIFF_SCOPE | grep -nE '(#|//) ?SHORTCUT:'`. Every `SHORTCUT:` marker the change introduces must name both a ceiling and an upgrade trigger; a marker with no trigger is a **should_fix**. List all unresolved markers in the Step 3 report so deferrals stay visible.
6. **Build** — Clean build, zero errors.
7. **File length** — Changed production files (non-test): >800 lines consider splitting, >1000 flag for review.
8. **Plan verify commands** — Run any task-specific verify command not already covered by current evidence. Preserve exit status and output; choose a timeout appropriate to the operation, and track a still-running process through its handle rather than interpreting an observation timeout as failure. Defer commands requiring a running service or browser to Phase B.
9. **Performance audit** — For each changed file on a hot path (UI render, request handler, polling loop, CLI inner loop): is expensive work (parsing, serialization, I/O, dependency loading) cached/memoized? Are heavy dependencies imported fully when lighter alternatives exist? Does repeated invocation redo work when input hasn't changed? **This is a static code review — no running program needed.** Performance issues from missing caching are structural and visible in the source.
10. **Feature Parity Check (migration/refactoring only)** — Skip unless the plan has a `## Feature Inventory` section.
    1. Compare old vs new implementation
    2. Verify each feature exists in new code
    3. Run new code and verify same behavior

    <!-- CC-ONLY -->
    **If features are MISSING:** Run the iteration-cap check from Step 11 first (read `Iterations:` from the plan header; if `>= 3` ask the user Continue / Pivot / Abandon before incrementing). On Continue: add tasks with `[MISSING]` prefix, set `Status: PENDING`, increment `Iterations`, register status change, invoke `Skill(skill='spec-implement', args='<plan-path> $LANE_FLAG')`.
    <!-- /CC-ONLY -->
    <!-- CODEX-START
    **If features are MISSING:** Run the iteration-cap check from Step 11 first (read `Iterations:` from the plan header; if `>= 3` present the user with Continue / Pivot / Abandon options before incrementing). On Continue: add tasks with `[MISSING]` prefix, set `Status: PENDING`, increment `Iterations`, register status change, then continue immediately with the `$spec-implement` skill instructions using arguments: `<plan-path> $LANE_FLAG`.
    CODEX-END -->
